global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Once the FastAPI app is running, it will expose metrics here
  - job_name: 'parallax_api'
    static_configs:
      - targets: ['host.docker.internal:8000']
