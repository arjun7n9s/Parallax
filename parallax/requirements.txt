# ============================================================
# PARALLAX - Complete Requirements
# Organized by system layer per 03_TECH_STACK.md
# ============================================================

# Layer 1: Ingestion & Triage
# Pin fastapi + starlette together: an unpinned starlette resolved a combo on CI
# where FastAPI routing hit '_IncludedRouter' has no attribute 'path'. These are
# the versions the suite passes on.
fastapi==0.136.3
starlette==1.2.1
uvicorn[standard]>=0.30.0
celery[redis]>=5.4.0
redis>=5.0.0
minio>=7.2.7
apkid>=3.0.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
alembic>=1.13.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.10
httpx>=0.27.0
httpcore==1.0.5
asyncpg>=0.29.0

# Layer 2A: Static Analysis
androguard>=4.1.0
networkx>=3.3
yara-python>=4.5.1

# Layer 2B: Dynamic Analysis
# frida 16.x: 17.x removed the bundled Java/ObjC bridges, so every Java.perform
# hook fails with "Java is not defined". 16.x keeps the classic Java global.
frida==16.7.19
# frida-tools 14.x needs frida 17.x; cap below 14 to stay paired with frida 16.x.
frida-tools>=12.0.0,<14
mitmproxy>=11.0.0
scapy>=2.5.0
adb-shell>=0.4.4
pure-python-adb

# Layer 2C: Visual Intelligence
opencv-python>=4.10.0
pillow>=10.4.0
sentence-transformers>=3.1.0

# Layer 3: AI Reasoning Cortex
ollama>=0.3.0
langgraph>=0.2.0
langchain>=0.2.16
langchain-community>=0.2.16
crewai>=0.80.0
llama-index>=0.12.0
llama-index-llms-ollama>=0.3.0
dspy>=2.5.0
openai>=1.51.0
anthropic>=0.36.0

# Layer 4: Knowledge Graph & Vector
neo4j>=5.24.0
qdrant-client>=1.10.0
pymisp>=2.4.200

# Layer 5: Delivery & Reports
jinja2>=3.1.4
weasyprint>=63.1
reportlab>=4.2.2
stix2>=3.0.1

# Observability
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-celery>=0.48b0
opentelemetry-exporter-otlp>=1.27.0
prometheus-client>=0.21.0
structlog>=24.4.0

# Utilities
python-dotenv>=1.0.1
tenacity>=9.0.0
pyyaml>=6.0.0
rich>=13.9.0
tqdm>=4.66.0

# Testing & Quality
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
mypy>=1.11.0
ruff>=0.6.0

# Pin <4.1: bcrypt 4.1+ removed __about__, breaking passlib (mitmproxy proxyauth import).
bcrypt==4.0.1
