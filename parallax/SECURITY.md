# Security

Security documentation for PARALLAX lives in the MkDocs site:

- `docs/security.md` for the bank audit overview
- `docs/admin/configuration.md` for runtime secrets and settings
- `docs/runbooks/backup_restore.md` for backup and restore operations

Build the docs with:

```bash
mkdocs build --strict
```
